#include <algorithm>
#include <cmath>
#include <iostream>
#include <string>
#include <vector>

int main(int argc, char * argv[])
{
#ifndef YOLO
	std::ios_base::sync_with_stdio(false);
#endif

	size_t count;
	std::cin >> count;

    return 0;
}
